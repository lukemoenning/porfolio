import styled from 'styled-components'

import { BodyContentWide } from '@/app/libs/common-styles'

export const ProjectsWrapper = styled(BodyContentWide)`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  justify-content: space-evenly;
  margin-top: 30px;
`
